{
  "id": "python-fullstack",
  "name": "Python Full-Stack",
  "description": "Django / FastAPI with pip and Git",
  "requirements": [
    { "tool": "python", "versionRange": ">=3.10.0", "required": true },
    { "tool": "git", "versionRange": ">=2.30.0", "required": true }
  ]
}
